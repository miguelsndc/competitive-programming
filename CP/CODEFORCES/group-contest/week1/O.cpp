#include <bits/stdc++.h>

using namespace std;

#define ll long long
#define MOD 1000000007
#define INF 1000000009

void solve() {
    int n; cin >> n; 
    vector<int> max_h(n), left(n), right(n);
    ll sum = 0;
    for (int i = 0; i < n; i++) {
        cin >> max_h[i]; sum += max_h[i];
    }
}

int main() 
{
    ios_base::sync_with_stdio(0);
    cin.tie(0);
    int tt = 1; // cin >> tt;
    while(tt--) {
         solve();
    }
}